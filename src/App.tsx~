import { MainLayout } from "@/components/layout/MainLayout.tsx";
import { Timer } from "@/components/timer/Timer.tsx";
import { ApolloProvider } from "@apollo/client";
import { client } from "@/apolloClient.ts";
import { VideostandEvents } from "@/components/videostandEvents/VideostandEvents.tsx";
import { useVideostandEvents } from "@/hooks/useVideostandEvents.ts";
import { CurrentEvent } from "@/components/currentEvent/CurrentEvent.tsx";
import { MainScreen } from "@/components/screens/MainScreen";

function App() {
  return (
    <ApolloProvider client={client}>
      <MainScreen />
    </ApolloProvider>
  );
}

export default App;
